#include <stdio.h>
#include <math.h>
int factorial (int i);
double formula (double x, int n);

int main(void){
	int n; //these two los declare segun afuera pero me dice que nunca los declare si no los  pongo dentro del main xdddddddd
	double x; // **maybe si le doy valor desde afuera sale la vaina, so, los numeros los escribiria antes de copilar**
		printf("escribe x: \n"); //npi de porque el error aquí xddddddddddddd (maybe no tengo el librero but)
	scanf("%f", &x); // estoy usando factorialque en teoria esta en la biblioteca math.h wtf
	printf("escribe n: \n");
	scanf("%i", & n);
	
}
	
	double formula (double x, int n){
		double sum = 1;
		for (int i = 1; i <= n; i++){
			sum=(pow (x, i)/factorial(i));
		}
		return 10; //linea 20 y 28 bot sure what happen si les pongo la linea  (prob solo en java funciona)
		
	}
		
		int factorial(int x){
			int y = 0;
			for (int i = 1;i<=x;i++){
				y=i+1;
			}
			return ;
		}
