#include <stdio.h>
// quedo del otro lado but it works xddd solo que la altura es un numero menos :v
int main(){
int m,n,i;
printf("altura de la figura \n");
fflush(stdin);
scanf("%i", &n);
for(i=1;i<=n;i++){
    for(m=1;m<=n-i;m++){
        printf("*");}
        printf("\n");}}

