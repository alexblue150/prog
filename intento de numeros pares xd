#include <stdio.h>
#include <conio.h>

int main(){

int a,n,sumadorent=0;
printf("escribe el numero \n");
scanf("%d",&n);
while(n>0){
        a=n%10;
        n=n/10;
        if (n = 2 || 4 || 6 || 8);
        sumadorent++;

}
printf("numero de paresxaxaxaxax: %d", sumadorent);
return 0;
}
